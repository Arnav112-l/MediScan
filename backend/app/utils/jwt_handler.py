"""JWT helpers — Flask-JWT-Extended is configured on the app; this module documents patterns."""
from __future__ import annotations

# Use @jwt_required(), @jwt_required(optional=True), create_access_token from flask_jwt_extended.
# Identity is stored as string user id.
